"""
NBA API wrapper

Created because existing solution either didn't work or were overcomplicated.
I might move it into a separate package at some point but I probably won't
ever implement _all_ the endpoints, only the ones I need.
"""
from nba_api.api import NBA  # noqa
